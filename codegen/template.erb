@interface AMQProtocol<%= class_name %><%= method_name %> : MTLModel <NSCoding, <%= protocols.join(", ") %>>
<%- fields.each do |f| -%>
@property (nonnull, copy, nonatomic, readonly) <%= f[:type] %> *<%= f[:name] %>;
<%- end -%>
<%= constructor %>
@end

